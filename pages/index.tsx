import type { NextPage } from "next";
import MainReport from "components/main/MainReport";
const Home: NextPage = () => {
  return (
    <div>
      <MainReport />
    </div>
  );
};

export default Home;
