export interface ResponseTotalNumber {
  totalNumber: number | null;
}

export interface LectureTotalNumber {
  lectureTotalNumber: number | null;
}

export interface ResponseFindNumber {
  findNumber: number | null;
}

export interface LectureFindNumber {
  lectureFindNumber: number | null;
}

export interface ResponseRequestNumber {
  requestNumber: number | null;
}

export interface LectureRequestNumber {
  lectureRequestNumber: number | null;
}
